// incluir libreria
#include <stdio.h>

void main(void)
{
    // Declaracion de variable
    int i = 15, j, k, l;


    // declaracion fucion de la variable
    j = (15 > i--) > (14 < ++

                      // imprimir en pantallas el valor e n pantalla de j
    printf("\nEl valor de j es: %d", j);

         // imprimir en pantallar el valor de k
    k = ! ('b' != 'd') > (!i - 1);
    printf("\nEl valor de k es: %d", k);

                        // imprimir en pantalla el valor de 1
    l = (! (34 > (70 % 2)) ||0);
    printf("\nEl valor de l es: %d", l);

}
